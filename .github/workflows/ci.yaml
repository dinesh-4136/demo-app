name: CI - Docker Build and Test

on:
<<<<<<< HEAD
push:
  branches: [main]
pull_request:
  branches: [main]
=======
  push:
    branches: [main]
  pull_request:
    branches: [main]
>>>>>>> dfbb2ce1212e072d99d7701bb779dbe096e88b0c

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install depedencies
        run: npm install

      - name: Run unit test
        run: npm test

      - name: Run linter
        run: npm run lint

      - name: Generate code coverage
        run: npm run test -- --coverage

      - name: Build Docker Image
        run: docker build -t demo-app .
